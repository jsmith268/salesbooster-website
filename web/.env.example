# Copy to .env.local for local development. Do not commit .env.local.

# Public site URL — used by metadataBase, OG images, sitemap, canonical URLs.
NEXT_PUBLIC_SITE_URL=https://salesbooster.app

# Form submission backend (placeholders; not wired yet — see planning.md).
# When you wire /waitlist and /apply to a real backend, fill these in:
# RESEND_API_KEY=
# DATABASE_URL=
# WAITLIST_NOTIFY_EMAIL=
